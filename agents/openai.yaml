schema_version: 1
display_name: Moment Notes
short_description: Emotional photo annotations with scene-aware doodle notes.
default_prompt: For every uploaded image, generate the annotated image first with meaningful hand-drawn notes, preserve the original filter and color mood, process multiple images separately by default, and only add summary plus follow-up suggestions after the image result.
